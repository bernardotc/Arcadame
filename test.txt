Program Example2:
    func multiplyTable(int: n) = int:
        var int: count;
        var int[10]: arr;
        count = 0;
        while (count < 10):
            arr[count] = (count + 1) * n;
            count = count + 1;
        end;
        count = 0;
        while (count < 10):
            print(arr[count]);
            count = count + 1;
        end;
    end
    Main:
        print("Times Tables");
        multiplyTable(2);
        print("-------------");
        multiplyTable(3);
    end
End
